# 深梦扬帆房源监控

一个帮助应届毕业生关注深圳免费过渡住宿房源的开源 Demo，主要适配微信“安居乐寓”小程序里的“深梦扬帆”公益活动。

“深梦扬帆”为符合活动条件的来深应届毕业生提供 **15 天免费住宿**，帮助刚到深圳的年轻人度过求职和落脚的第一阶段。具体申请资格、入住期限和活动规则以官方当期说明为准。

申请房源时不必反复刷新小程序。程序在你的电脑本地定时检查房源，发现关注区域有新房或余量增加时发送邮件；如果收件邮箱已经开启微信提醒，就能直接在微信里看到消息。

本项目只做公开房源信息的监控和通知，不会自动登录、自动报名、自动预约或代抢。最终房源和活动规则请以官方小程序为准。

## 为什么做这个 Demo

应届毕业生刚来深圳时，往往同时面对找工作、短期住宿和长期租房。活动提供的 15 天免费住宿能减轻刚到深圳时的压力，但房源数量有限，手动反复查看既耗时间，也容易错过更新。

这个 Demo 希望把重复查看的工作交给电脑：

- 自己选择关注的行政区
- 每 10 分钟检查一次
- 记录房源增加、减少、满租和重新开放
- 南山区出现新房时发送邮件
- 所有数据和邮箱设置只保存在自己的电脑里

它不是官方产品，也不提供任何抢房特权，只是一个个人提醒工具。

## 工作流程

1. 本地程序按设定时间检查全部社区。
2. 将本次结果与上一次记录比较。
3. 只关注你在配置中选择的行政区。
4. 房源从无到有、余量增加，或申请窗口重新开放且有房时发送邮件。
5. 邮件可发送到 QQ 邮箱等能在微信中提醒的邮箱。

默认在每小时的 `01、11、21、31、41、51` 分检查，既保持每 10 分钟一次，也覆盖 `10:01、14:01、22:01` 等常见放房时间。

## 特点

- 支持同时关注多个行政区
- 检测房源新增、减少、满租和开放状态变化
- 邮件包含社区、街道、楼栋和最新余量
- 同一批变化只提醒一次
- 邮件发送失败时不会覆盖旧记录，下次检查可以再次发现
- 仅使用 Python 自带功能，不需要安装第三方包
- 支持 Windows、macOS 和 Linux

## 不会写代码？让 AI 在本地帮你做

你不一定要研究下面的安装和配置。只要准备好小程序和邮箱，就可以让本地编程助手完成整个项目。

### 准备

1. 在微信中打开并登录“安居乐寓”小程序，进入“深梦扬帆”房源页面。
2. 在电脑上登录准备用来发信的 Gmail。
3. 准备一个接收提醒的邮箱；如果希望在微信里直接看到消息，请先给这个邮箱开启微信提醒。
4. 打开 Codex，推荐选择 **GPT-5.6 Sol**，推理强度选择 **极高**。
5. 新建一个空文件夹，把下面的提示词发给它。

### 可以直接复制的提示词

> “深梦扬帆”是帮助应届毕业生来深圳免费住宿 15 天的公益活动。请查看我已经打开的微信“安居乐寓—深梦扬帆”房源页面，在当前文件夹创建一个只在本地运行的房源监控工具。让我选择需要关注的行政区、街道或社区，每 10 分钟检查一次，记录房源增加和减少；关注区域出现新房时，用我已登录的 Gmail 给我指定的、能在微信中提醒的邮箱发消息。程序要有“立即检查”“发送测试邮件”“开始监控”和“停止监控”按钮，只负责提醒，不自动报名。个人邮箱和登录信息不得上传到 GitHub。需要登录或授权时再让我操作，其他步骤请你完成，并在交付前实际运行测试。最后整理好 README，让我可以直接发布到 GitHub。

AI 完成后，你只需要点击“发送测试邮件”，确认邮箱或微信能够收到，再启动监控即可。

### 其他房源小程序也能这样做

这段提示词不只适用于“安居乐寓”。使用其他房源小程序时，把提示词中的小程序名称、关注区域和提醒条件替换掉，再让 AI 先查看对应页面即可。

不同小程序的页面和规则可能不同。如果 AI 无法读取页面，应让它停下来请你协助登录或打开目标页面，不要尝试绕过登录、验证码或访问限制。

## 手动安装和运行

需要 Python 3.10 或更高版本。

### 1. 准备配置

复制示例文件：

```powershell
Copy-Item config.example.json config.json
```

打开 `config.json`，修改关注区域：

```json
{
  "watch_districts": ["南山区"]
}
```

也可以同时关注多个区域：

```json
{
  "watch_districts": ["南山区", "福田区"]
}
```

请保留示例文件中的其他设置。

### 2. 设置 Gmail

Gmail 通常需要先开启两步验证，再创建应用专用密码。不要把 Gmail 密码或应用专用密码写进配置文件、代码或提交到 GitHub。

复制邮件设置示例：

```powershell
Copy-Item .env.example .env
```

打开 `.env`，填写以下三项：

```text
HOUSING_SMTP_USER=你的Gmail地址
HOUSING_SMTP_APP_PASSWORD=你的Gmail应用专用密码
HOUSING_NOTIFY_TO=接收提醒的邮箱地址
```

也可以只在当前 PowerShell 窗口临时设置：

```powershell
$env:HOUSING_SMTP_USER="你的Gmail地址"
$env:HOUSING_SMTP_APP_PASSWORD="你的Gmail应用专用密码"
$env:HOUSING_NOTIFY_TO="接收提醒的邮箱地址"
```

如果接收邮箱已经开启微信提醒，邮件到达后就能在微信中看到通知。

### 3. 打开本地窗口

Windows 用户可以双击 `start_windows.bat`，也可以运行：

```powershell
python app.py
```

窗口提供以下按钮：

- `发送测试邮件`：检查邮件通道是否可用
- `立即检查一次`：马上读取一次当前房源
- `开始持续监控`：按设定时间自动检查
- `停止监控`：停止自动检查

测试按钮显示“发送成功”代表 Gmail 已接受邮件，请再检查收件箱或微信提醒确认是否到达。

### 4. 命令行测试邮件

```powershell
python monitor.py --test-email
```

### 5. 测试房源读取

```powershell
python monitor.py --once --dry-run
```

### 6. 命令行开始监控

```powershell
python monitor.py
```

程序窗口需要保持运行，电脑进入睡眠或关机后监控会暂停。

## 常用设置

`config.json` 中可以修改：

| 设置 | 作用 |
| --- | --- |
| `watch_districts` | 需要邮件提醒的行政区 |
| `check_minutes` | 每小时在哪些分钟检查 |
| `notify_on_startup` | 第一次启动时，现有房源是否也发邮件 |
| `email.enabled` | 是否启用邮件提醒 |

默认 `notify_on_startup` 为 `false`，第一次运行只建立基准，不会把已经存在的房源当成刚释放的房源。

## 命令

```text
python monitor.py                 持续监控
python monitor.py --once          只检查一次
python monitor.py --once --dry-run  不发邮件，显示结果和邮件预览
python monitor.py --test-email    发送测试邮件
```

## 隐私与安全

- `config.json`、`.env` 和本地房源记录已经加入忽略列表，不会被正常提交。
- 邮箱密码只从电脑的环境变量读取。
- 发布代码前仍建议检查提交内容，确认没有个人邮箱、密码和本地记录。

## 使用边界

本项目用于个人信息提醒。请合理设置检查频率，遵守相关服务的使用规则，不要用它进行高频请求、自动报名、批量注册、绕过验证或其他影响正常服务的操作。

房源接口和活动规则可能调整，出现异常时请以官方小程序为准。

## 开源协议

[MIT](LICENSE)
